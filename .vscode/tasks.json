{
    "version": "2.0.0",
    "tasks": [
        {
            "problemMatcher": "$gcc",
            "type": "shell",
            "group": "build",
            "label": "cmake build and flash",
            "command": "ninja -C ${command:cmake.buildDirectory} burn-${command:cmake.launchTargetFilename}"
        }
    ]
}